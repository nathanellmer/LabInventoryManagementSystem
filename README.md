# LabInventoryManagementSystem
LIMS is a database system with a user interface built to make taking stock simpler.

# Before use
Remember to utilise a personal venv which can be setup using "python -m venv venv". After activation and hence within this venv, LIMS requires the use of two packages: PySide6 and openpyxl.
